# camera-app
let's create a new app that takes pictures and upscales them with ai (use an accessible model) or upscales saved images. and uses an ffmpeg lib to save as any format or convert saved images to any format
